import { createAction } from '@ngrx/store';


export const plusDollar = createAction('[Counter Component] Increment');
export const minusDollar = createAction('[Counter Component] Decrement');
export const reset = createAction('[Counter Component] Reset');
